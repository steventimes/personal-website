---
interface Props {
  skills: Record<string, string[]>;
}

const { skills } = Astro.props;
---
<div class="grid md:grid-cols-2 gap-5">
  {Object.entries(skills).map(([group, items]) => (
    <div class="glass shadow-soft rounded-2xl p-5">
      <p class="font-semibold mb-3 text-slate-900 dark:text-slate-100">{group}</p>
      <div class="flex flex-wrap gap-2">
        {items.map((s) => (
          <span class="text-sm px-3 py-1 rounded-full bg-slate-900/5 dark:bg-white/10 border border-slate-900/10 dark:border-white/10 text-slate-900 dark:text-slate-100">
            {s}
          </span>
        ))}
      </div>
    </div>
  ))}
</div>
